name: prospector
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip:
    - fastapi
    - httpx
    - playwright
    - pydantic
    - pydantic-settings
    - pytest
    - pyyaml
    - requests
    - sortedcontainers
    - tenacity
    - uvicorn
prefix: prospector
